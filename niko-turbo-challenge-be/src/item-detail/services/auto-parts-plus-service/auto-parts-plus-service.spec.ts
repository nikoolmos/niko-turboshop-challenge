import { AutoPartsPlusService } from './auto-parts-plus-service';

describe('AutoPartsPlusService', () => {
  it('should be defined', () => {
    expect(new AutoPartsPlusService()).toBeDefined();
  });
});
