import { RepuestosMaxService } from './repuestos-max-service';

describe('RepuestosMaxService', () => {
  it('should be defined', () => {
    expect(new RepuestosMaxService()).toBeDefined();
  });
});
