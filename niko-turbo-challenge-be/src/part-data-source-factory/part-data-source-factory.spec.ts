import { PartDataSourceFactory } from './part-data-source-factory';

describe('PartDataSourceFactory', () => {
  it('should be defined', () => {
    expect(new PartDataSourceFactory()).toBeDefined();
  });
});
